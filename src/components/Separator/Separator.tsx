import React from 'react';
import {StyleSheet, View} from 'react-native';

export const Separator = () => <View style={styles.sep} />;

const styles = StyleSheet.create({
  sep: {
    marginVertical: 4,
  },
});
